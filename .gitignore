__pycache__/
*.pyc
.DS_Store
node_modules/
.env
*.swp
*.swo
.idea/
.vscode/
.venv/
.coverage
htmlcov/
*.egg-info/
dist/
build/
.pytest_cache/

# Stray SQLite ':memory:' databases accidentally written to disk when
# a Python script passes the literal string ":memory:" as a *path*
# instead of using it as a connection argument. SQLite happily creates
# a file with that name in the current working directory.
*:memory:
